Comando:
rg -n "gh[pousr]_[A-Za-z0-9]{20,}|AKIA[0-9A-Z]{16}|-----BEGIN (RSA |EC |OPENSSH )?PRIVATE KEY-----|EXPO_PUBLIC_[A-Z0-9_]*(SECRET|PRIVATE_KEY|ACCESS_TOKEN)\s*=" -S . --glob "!node_modules/**" --glob "!dist/**" --glob "!coverage/**"

Resultado observado:
No matches found.

Interpretacion:
No se encontraron patrones de token de GitHub, access key de AWS, llaves privadas ni variables EXPO_PUBLIC con nombres de secreto en los archivos revisados.
